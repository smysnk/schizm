{{- $secret := default (dict) .Values.api.secret -}}
{{- if and (not .Values.api.existingSecret) (gt (len $secret) 0) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "schizm.apiSecretName" . }}
  labels:
    {{- include "schizm.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
type: Opaque
stringData:
  {{- range $key := keys $secret | sortAlpha }}
  {{ $key }}: {{ printf "%v" (index $secret $key) | quote }}
  {{- end }}
{{- end }}
