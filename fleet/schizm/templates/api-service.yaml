apiVersion: v1
kind: Service
metadata:
  name: {{ include "schizm.apiName" . }}
  labels:
    {{- include "schizm.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: {{ .Values.api.service.type }}
  selector:
    {{- include "schizm.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      {{- if and .Values.api.service.nodePort (or (eq .Values.api.service.type "NodePort") (eq .Values.api.service.type "LoadBalancer")) }}
      nodePort: {{ .Values.api.service.nodePort }}
      {{- end }}
